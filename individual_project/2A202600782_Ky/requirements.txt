# Core
python-dotenv>=1.0.0
requests>=2.31.0

# Task 2 - Crawling
crawl4ai>=0.3.0

# Task 3 - Document conversion
markitdown[pdf,docx]>=0.1.0
# Nguồn legal dạng .doc cũ (OLE2) -> convert sang .docx bằng LibreOffice headless
# (binary `soffice`) trước khi đưa vào MarkItDown. Cài: libreoffice-still.
# OCR fallback giữ lại cho trường hợp PDF scan không có text layer.
# Cần tesseract binary + vie.traineddata (tải sẵn vào ./tessdata/).
pytesseract>=0.3.13
pdf2image>=1.17.0
pillow>=10.0.0

# Task 4 - Chunking & Indexing
langchain-text-splitters>=0.2.0
sentence-transformers>=2.2.0
weaviate-client>=4.5.0
# chromadb>=0.4.0        # Alternative
# faiss-cpu>=1.7.4       # Alternative

# Task 6 - BM25
rank-bm25>=0.2.2
pyvi>=0.1.1            # Word-segmentation tiếng Việt cho BM25 (tàng trữ, ma túy)

# Task 7 - Reranking
# Cross-encoder local BAAI/bge-reranker-v2-m3 chạy qua sentence-transformers
# (đã khai báo ở Task 4) -> không cần thêm package; chỉ cần tải weights lần đầu.

# Task 8 - PageIndex
pageindex>=0.1.0

# Task 10 - Generation
openai>=1.30.0
# google-generativeai>=0.5.0  # Alternative: Gemini

# Group project - UI
streamlit>=1.35.0
# gradio>=4.30.0         # Alternative
# chainlit>=1.0.0        # Alternative for chatbot

# Group project - Evaluation (choose one)
deepeval>=1.0.0
# ragas>=0.1.0           # Alternative
# trulens>=1.0.0         # Alternative
